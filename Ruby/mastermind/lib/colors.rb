require_relative 'board' # to access board@valid_colors

# Handles returning guesses colored properly
module Colors
  def Colors.make_colored(input)
    # Use colorize to take in an array of characters and return an array of the same length, except each item is now colorized
  end
end